import React, { useEffect } from 'react';

const AntPath: React.FC<{
  $layer: React.MutableRefObject<any>;
  events: any[];
}> = function({ $layer, events }) {
  useEffect(() => {}, []);

  return null;
};

export default AntPath;
